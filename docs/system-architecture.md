# 🚀 GCLI2API 项目系统架构

> **文档版本**: 1.0.0  
> **生成日期**: 2026-01-22  
> **作者**: Antigravity AI Assistant

---

## 📋 项目概述

**GCLI2API** 是一个**统一 API 网关系统**，用于将多个 AI 模型服务后端（如 Google Gemini/Claude via Antigravity、GitHub Copilot、Kiro Gateway 等）聚合到单一 API 入口，提供：

- **统一入口**：将多个后端服务整合到单一 API 端点
- **智能路由**：根据模型名称、优先级自动选择最优后端
- **故障转移**：主后端失败时自动切换到备用后端
- **多格式兼容**：支持 OpenAI、Anthropic Messages、Gemini 原生等多种 API 格式
- **凭证管理**：自动管理 Google OAuth 凭证，支持轮换和冷却机制

---

## 🏗️ 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端层 (Clients)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  Cursor IDE  │  Augment Code  │  Claude CLI  │  OpenAI SDK  │  其他客户端   │
└───────┬──────┴───────┬────────┴──────┬───────┴──────┬───────┴───────┬───────┘
        │              │               │              │               │
        ▼              ▼               ▼              ▼               ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FastAPI 主服务 (web.py)                            │
│                         运行在 Hypercorn ASGI 服务器                          │
│                              默认端口: 7861                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                           IDE 兼容中间件层                                    │
│                        (IDECompatMiddleware)                                 │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                              路由器层 (Routers)                                │
├───────────────────┬───────────────┬───────────────┬───────────────────────────┤
│   OpenAI Router   │ Gemini Router │ Antigravity   │  Antigravity Anthropic    │
│  /v1/chat/...     │ /v1/models/   │   Router      │      Messages Router      │
│                   │ :generate...  │ /antigravity/ │  /antigravity/v1/messages │
├───────────────────┴───────────────┴───────────────┴───────────────────────────┤
│                        统一网关路由器 (Unified Gateway Router)                  │
│                              /gateway/v1/...                                   │
│                     支持 OpenAI 和 Anthropic 格式                              │
├───────────────────────────────────────────────────────────────────────────────┤
│                            Web 控制台路由器                                     │
│                         /auth/*, /panel/*, /ws/*                               │
└───────────────────────────────────┬───────────────────────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                              核心服务层                                         │
├───────────────────┬───────────────┬───────────────┬───────────────────────────┤
│   Credential      │   Routing     │    Proxy      │     Message               │
│   Manager         │   Decision    │    Module     │     Converter             │
│  (凭证管理)        │  (路由决策)    │   (代理层)     │    (格式转换)              │
└───────────────────┴───────────────┴───────────────┴───────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                              后端服务层 (Backends)                              │
├───────────────────┬───────────────┬───────────────┬───────────────────────────┤
│   Antigravity     │    Copilot    │ Kiro Gateway  │     AnyRouter             │
│   (Priority: 1)   │  (Priority: 4)│ (Priority: 2) │    (Priority: 3)          │
│   本地直调         │  HTTP 代理    │   HTTP 代理   │     HTTP 代理             │
│   127.0.0.1:7861  │ 127.0.0.1:8141│ 127.0.0.1:9876│   外部公益站               │
└───────────────────┴───────────────┴───────────────┴───────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                              外部服务层                                         │
├───────────────────┬───────────────┬───────────────┬───────────────────────────┤
│  Google Gemini    │  GitHub       │  Kiro Claude  │   Third-party             │
│  / Claude API     │  Copilot API  │     API       │      APIs                 │
│  (via OAuth)      │  (via Token)  │               │                           │
└───────────────────┴───────────────┴───────────────┴───────────────────────────┘
```

---

## 📁 项目目录结构

```
gcli2api/
├── web.py                    # 🚀 主入口文件，集成所有路由
├── config.py                 # ⚙️ 配置管理模块
├── log.py                    # 📝 日志系统
│
├── config/
│   └── gateway.yaml          # 🔧 网关配置文件（后端、路由规则）
│
├── src/
│   ├── __init__.py
│   │
│   │── # ==================== 核心路由模块 ====================
│   ├── openai_router.py              # OpenAI 兼容路由
│   ├── gemini_router.py              # Gemini 原生路由
│   ├── antigravity_router.py         # Antigravity API 路由
│   ├── antigravity_anthropic_router.py  # Anthropic Messages 格式路由
│   ├── unified_gateway_router.py     # 统一网关路由 (核心)
│   ├── web_routes.py                 # Web 控制台路由
│   │
│   │── # ==================== 网关子模块 ====================
│   ├── gateway/
│   │   ├── router.py         # 主路由
│   │   ├── backends.py       # 后端管理
│   │   ├── routing.py        # 路由决策
│   │   ├── proxy.py          # 代理层
│   │   └── backends/         # 后端实现
│   │       ├── antigravity.py
│   │       ├── copilot.py
│   │       └── kiro.py
│   │
│   │── # ==================== 格式转换模块 ====================
│   ├── converters/
│   │   ├── message_converter.py      # OpenAI ↔ Antigravity 消息转换
│   │   └── tool_converter.py         # 工具调用转换
│   ├── anthropic_converter.py        # Anthropic 格式转换
│   ├── anthropic_streaming.py        # Anthropic 流式响应转换
│   ├── openai_transfer.py            # OpenAI/Gemini 格式双向转换
│   │
│   │── # ==================== 凭证管理模块 ====================
│   ├── auth.py                       # OAuth 2.0 认证流程
│   ├── credential_manager.py         # 多凭证管理和轮换
│   ├── google_oauth_api.py           # Google OAuth API
│   │
│   │── # ==================== 其他核心模块 ====================
│   ├── httpx_client.py               # HTTP 客户端管理
│   ├── antigravity_api.py            # Antigravity API 调用
│   ├── fallback_manager.py           # 降级管理
│   ├── rate_limiter.py               # 速率限制
│   ├── token_estimator.py            # Token 估算
│   └── signature_cache.py            # 签名缓存
│
├── creds/                    # 凭证存储目录
│   ├── *.json               # Google OAuth 凭证文件
│   └── antigravity/         # Antigravity 专用凭证
│
└── front/                    # Web 控制台前端
    └── index.html
```

---

## 🔀 路由系统

### 路由器概览

| 路由器 | 前缀 | 主要端点 | 功能 |
|--------|------|----------|------|
| **OpenAI Router** | `/v1` | `/v1/chat/completions`, `/v1/models` | OpenAI 格式请求处理 |
| **Gemini Router** | `/v1/models` | `/{model}:generateContent` | Gemini 原生格式处理 |
| **Antigravity Router** | `/antigravity` | `/antigravity/v1/chat/completions` | Antigravity API 路由 |
| **Anthropic Router** | `/antigravity` | `/antigravity/v1/messages` | Anthropic Messages 格式 |
| **Unified Gateway** | `/gateway` | `/gateway/v1/chat/completions`, `/gateway/v1/messages` | 统一网关（核心） |
| **Web Router** | `/` | `/auth/*`, `/panel/*`, `/ws/*` | Web 控制台 |

### 智能路由决策

路由决策按以下优先级进行：

```
1. 模型特定路由规则 (config/gateway.yaml 的 model_routing)
   ↓
2. Kiro Gateway 路由 (环境变量 KIRO_GATEWAY_MODELS)
   ↓
3. Antigravity 支持列表检查
   ↓
4. 默认路由：Antigravity → Copilot
```

### 路由决策流程图

```
                    ┌─────────────────────┐
                    │   接收请求 (model)   │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ 检查 model_routing  │
                    │    配置规则         │
                    └──────────┬──────────┘
                               │
                    ┌──────────┴──────────┐
                    │ 有匹配规则?          │
                    └──────────┬──────────┘
                      是 │           │ 否
                         ▼           ▼
              ┌─────────────┐  ┌─────────────────┐
              │ 使用规则中的 │  │ 检查 Kiro Gateway│
              │ 后端优先级链 │  │    路由配置      │
              └─────────────┘  └────────┬────────┘
                                        │
                              ┌─────────┴─────────┐
                              │ Kiro 支持此模型?   │
                              └─────────┬─────────┘
                                是 │         │ 否
                                   ▼         ▼
                        ┌────────────┐ ┌─────────────────┐
                        │ Kiro Gateway│ │ Antigravity 支持?│
                        └────────────┘ └────────┬────────┘
                                          是 │       │ 否
                                             ▼       ▼
                                   ┌───────────┐ ┌────────┐
                                   │Antigravity│ │ Copilot│
                                   └───────────┘ └────────┘
```

---

## 🔄 后端服务

### 后端配置 (config/gateway.yaml)

| 后端 | 优先级 | Base URL | 支持模型 | 特点 |
|------|--------|----------|----------|------|
| **Antigravity** | 1 | `127.0.0.1:7861/antigravity/v1` | Claude 4.5, Gemini 3 | 本地直调，按 Token 计费 |
| **Kiro Gateway** | 2 | `127.0.0.1:9876/v1` | Claude 4.5/4 | Claude 专用降级 |
| **AnyRouter** | 3 | 外部 URL | 所有 Claude | 公益站第三方 API |
| **Copilot** | 4 | `127.0.0.1:8141/v1` | GPT-4/5, O1/O3 | 最终兜底，按次计费 |

### 后端支持模型详情

#### Antigravity 后端
- Claude 系列: `claude-sonnet-4.5`, `claude-opus-4.5`
- Gemini 系列: `gemini-3-pro`, `gemini-3-flash`
- GPT: `gpt-oos-120b`

#### Copilot 后端
- GPT-4 系列: `gpt-4`, `gpt-4o`, `gpt-4o-mini`, `gpt-4-turbo`
- GPT-5 系列: `gpt-5`, `gpt-5.1`, `gpt-5.2`
- O 系列: `o1`, `o1-mini`, `o1-preview`, `o3`, `o3-mini`

#### Kiro Gateway 后端
- Claude 4.5: `claude-sonnet-4.5`, `claude-opus-4.5`, `claude-haiku-4.5`
- Claude 4: `claude-sonnet-4`

### 降级链示例

#### Claude Sonnet 4.5 降级链
```
Kiro Gateway (claude-sonnet-4.5)
        ↓ 429/503/502/500/timeout
Antigravity (claude-sonnet-4.5)
        ↓
AnyRouter (claude-sonnet-4.5)
        ↓
Copilot (claude-sonnet-4.5)
        ↓
Copilot (claude-sonnet-4)
        ↓
Antigravity (gemini-3-pro)  ← 跨模型降级
```

#### Claude Opus 4.5 降级链
```
Antigravity (claude-opus-4.5)  ← Opus 优先使用 Antigravity
        ↓
Kiro Gateway (claude-opus-4.5)
        ↓
AnyRouter (claude-opus-4.5)
        ↓
Copilot (claude-opus-4.5)
        ↓
Copilot (claude-sonnet-4.5)
        ↓
Copilot (claude-sonnet-4)
        ↓
Antigravity (gemini-3-pro)
```

---

## 🔐 凭证管理

### 凭证管理器 (CredentialManager)

```python
class CredentialManager:
    """
    核心功能:
    - 多凭证文件管理
    - 自动 Token 刷新
    - 凭证轮换和冷却
    - 429/503 错误时自动切换凭证
    - 按模型分组的凭证隔离
    """
    
    async def get_valid_credential(
        self, 
        is_antigravity: bool = False,
        model_key: str = None
    ) -> Tuple[str, dict]:
        """获取有效凭证"""
        pass
    
    async def record_api_call_result(
        self,
        credential_name: str,
        success: bool,
        is_antigravity: bool = False
    ):
        """记录 API 调用结果"""
        pass
```

### 凭证存储结构

```
creds/
├── credential_1.json          # 普通 Google OAuth 凭证
├── credential_2.json
├── credential_3.json
└── antigravity/               # Antigravity 专用凭证
    ├── ag_credential_1.json
    └── ag_credential_2.json
```

### 凭证文件格式

```json
{
  "access_token": "ya29.xxx...",
  "refresh_token": "1//xxx...",
  "token_type": "Bearer",
  "expires_at": 1737561234,
  "project_id": "project-xxx"
}
```

### 凭证轮换策略

1. **正常轮换**: 每次请求随机选择一个可用凭证
2. **429 限流**: 标记当前凭证进入冷却期，切换到下一个凭证
3. **401 认证失败**: 尝试刷新 Token，失败则切换凭证
4. **503/529 服务过载**: 指数退避 + 切换凭证

---

## 📊 消息格式转换

### 支持的格式

```
┌─────────────┐     ┌─────────────────────┐     ┌─────────────────┐
│   OpenAI    │ ←→  │    Antigravity      │ ←→  │     Gemini      │
│   Format    │     │  (内部统一格式)       │     │    Format       │
└─────────────┘     └─────────────────────┘     └─────────────────┘
       ↑                      ↑
       │                      │
       ▼                      ▼
┌─────────────┐     ┌─────────────────────┐
│  Anthropic  │ ←→  │   Tool Calls        │
│  Messages   │     │   Conversion        │
└─────────────┘     └─────────────────────┘
```

### 核心转换函数

| 函数 | 位置 | 功能 |
|------|------|------|
| `openai_messages_to_antigravity_contents` | `converters/message_converter.py` | OpenAI → Antigravity |
| `convert_antigravity_stream_to_openai` | `antigravity_router.py` | 流式响应转换 |
| `antigravity_sse_to_anthropic_sse` | `anthropic_streaming.py` | Antigravity SSE → Anthropic SSE |
| `convert_messages_to_contents` | `anthropic_converter.py` | Anthropic → Antigravity |
| `gemini_contents_to_antigravity_contents` | `converters/message_converter.py` | Gemini → Antigravity |

### OpenAI 消息格式

```json
{
  "messages": [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Hello!"},
    {"role": "assistant", "content": "Hi there!"}
  ]
}
```

### Antigravity/Gemini 内容格式

```json
{
  "contents": [
    {
      "role": "user",
      "parts": [{"text": "Hello!"}]
    },
    {
      "role": "model",
      "parts": [{"text": "Hi there!"}]
    }
  ]
}
```

### Anthropic Messages 格式

```json
{
  "messages": [
    {
      "role": "user",
      "content": [{"type": "text", "text": "Hello!"}]
    },
    {
      "role": "assistant",
      "content": [{"type": "text", "text": "Hi there!"}]
    }
  ]
}
```

---

## 🌊 流式响应处理

### 流式处理流程

```
客户端请求 (stream=true)
        │
        ▼
┌───────────────────┐
│  Unified Gateway  │
│     Router        │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│   Proxy Module    │
│  (后端选择+代理)   │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│  Backend Service  │
│  (Antigravity等)  │
└────────┬──────────┘
         │ SSE Stream
         ▼
┌───────────────────┐
│  Stream Converter │
│  (格式转换)        │
└────────┬──────────┘
         │ Converted SSE
         ▼
    客户端接收
```

### SSE 格式

```
data: {"choices":[{"delta":{"content":"Hello"}}]}

data: {"choices":[{"delta":{"content":" world"}}]}

data: [DONE]
```

### 流式转换器

```python
async def convert_antigravity_stream_to_openai(
    lines_generator: Any,
    stream_ctx: Any,
    client: Any,
    model: str,
    request_id: str,
    credential_manager: Any,
    credential_name: str,
    ...
):
    """将 Antigravity 流式响应转换为 OpenAI 格式的 SSE 流"""
    async for line in lines_generator:
        # 解析 Antigravity SSE
        # 转换为 OpenAI 格式
        # yield 转换后的 SSE 行
        yield f"data: {json.dumps(openai_chunk)}\n\n"
```

---

## ⚙️ 配置管理

### 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `SERVER_PORT` | 7861 | 服务端口 |
| `SERVER_HOST` | 0.0.0.0 | 绑定地址 |
| `API_PASSWORD` | - | API 密码 |
| `COPILOT_ENABLED` | true | 是否启用 Copilot 后端 |
| `KIRO_GATEWAY_ENABLED` | false | 是否启用 Kiro Gateway |
| `KIRO_GATEWAY_MODELS` | - | Kiro 路由的模型列表 |
| `SONNET45_KIRO_FIRST` | true | Sonnet 4.5 是否优先使用 Kiro |
| `OPUS45_ANTIGRAVITY_FIRST` | true | Opus 4.5 是否优先使用 Antigravity |

### gateway.yaml 配置示例

```yaml
# 网关配置文件
# 位置: config/gateway.yaml

# ==================== 后端配置 ====================
backends:
  # Antigravity 后端 - 支持 Claude、Gemini 等模型
  antigravity:
    enabled: true
    priority: 1
    base_url: "http://127.0.0.1:7861/antigravity/v1"
    models: ["*"]  # 支持所有模型
    timeout: 60
    stream_timeout: 300
    max_retries: 2

  # Copilot 后端 - 支持 GPT、O1、O3 等 OpenAI 模型
  copilot:
    enabled: ${COPILOT_ENABLED:true}
    priority: 4
    base_url: "http://127.0.0.1:8141/v1"
    models:
      - gpt-4
      - gpt-4o
      - gpt-5
      - o1
      - o3
    timeout: 120
    stream_timeout: 600
    max_retries: 3

  # Kiro Gateway - Claude 专用
  kiro-gateway:
    enabled: ${KIRO_GATEWAY_ENABLED:false}
    priority: 2
    base_url: "http://127.0.0.1:9876/v1"
    timeout: 120
    stream_timeout: 600
    max_retries: 2

# ==================== 模型特定路由规则 ====================
model_routing:
  claude-sonnet-4.5:
    enabled: ${SONNET45_KIRO_FIRST:true}
    backends:
      - backend: kiro-gateway
        model: claude-sonnet-4.5
      - backend: antigravity
        model: claude-sonnet-4.5
      - backend: copilot
        model: claude-sonnet-4
      - backend: antigravity
        model: gemini-3-pro
    fallback_on:
      - 429
      - 503
      - 502
      - 500
      - timeout
      - connection_error
```

---

## 🔧 核心技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| Web 框架 | FastAPI | 0.100+ |
| ASGI 服务器 | Hypercorn | - |
| HTTP 客户端 | httpx | - |
| 异步 | asyncio | Python 3.12+ |
| 配置 | python-dotenv + YAML | - |
| 日志 | 自定义日志系统 | - |

---

## 🚀 启动流程

### 主入口 (web.py)

```python
async def main():
    """异步主启动函数"""
    from hypercorn.asyncio import serve
    from hypercorn.config import Config

    port = await get_server_port()  # 默认 7861
    host = await get_server_host()  # 默认 0.0.0.0

    config = Config()
    config.bind = [f"{host}:{port}"]
    config.keep_alive_timeout = 300
    config.read_timeout = 300
    config.write_timeout = 300

    await serve(app, config)
```

### 生命周期管理

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    """应用生命周期管理"""
    # 启动时
    await config.init_config()           # 初始化配置缓存
    await credential_manager.initialize() # 初始化凭证管理器
    await token_stats.init_db()          # 初始化 Token 统计
    
    yield
    
    # 关闭时
    await shutdown_all_tasks()           # 关闭所有后台任务
```

---

## 📝 API 端点一览

### OpenAI 兼容端点

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/v1/chat/completions` | 聊天完成 |
| GET | `/v1/models` | 获取模型列表 |

### Gemini 原生端点

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/v1/models/{model}:generateContent` | 生成内容 |
| POST | `/v1/models/{model}:streamGenerateContent` | 流式生成 |

### Antigravity 端点

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/antigravity/v1/chat/completions` | OpenAI 格式聊天 |
| POST | `/antigravity/v1/messages` | Anthropic 格式 |
| GET | `/antigravity/v1/models` | 获取模型列表 |

### 统一网关端点

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/gateway/v1/chat/completions` | 统一聊天端点 |
| POST | `/gateway/v1/messages` | Anthropic 格式 |
| GET | `/gateway/v1/models` | 获取所有后端模型 |

### Web 控制台端点

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/auth/start` | 开始 OAuth 认证 |
| POST | `/auth/callback` | OAuth 回调 |
| GET | `/panel/credentials` | 获取凭证列表 |
| WS | `/ws/logs` | 日志 WebSocket |

---

## 📈 监控与日志

### 日志系统

```python
from log import log

log.info("信息日志")
log.warning("警告日志")
log.error("错误日志")
log.success("成功日志", tag="GATEWAY")
log.route("路由日志", tag="GATEWAY")
```

### Token 统计

```python
from src import token_stats

# 记录 Token 使用
await token_stats.record_usage(
    model="claude-sonnet-4.5",
    input_tokens=1000,
    output_tokens=500,
    credential="cred_1"
)

# 获取统计
stats = await token_stats.get_stats()
```

---

## 📝 总结

GCLI2API 是一个功能完善的 **AI 模型 API 网关**，其核心价值在于：

1. **统一入口**：一个端点访问多个 AI 服务
2. **智能路由**：自动选择最优后端，支持配置化路由规则
3. **高可用**：多级故障转移，确保服务连续性
4. **格式兼容**：支持 OpenAI/Anthropic/Gemini 多种 API 格式
5. **凭证管理**：自动化的多凭证轮换和刷新机制

---

## 📚 相关文档

- [网关开发文档](./akari-gateway-development.md)
- [前端开发与打包指南](./akari-gateway-ui-and-packaging.md)
- [项目概述](./2026-01-22-gcli2api-project-overview.md)
